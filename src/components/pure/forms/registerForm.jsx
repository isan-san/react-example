import React, { useState } from "react";

const RegisterForm = () => {
  const initialData = {
    user: "",
    password: "",
    email:'',
    password:'',
  };
  const [data, setData] = useState(initialData);

  return <div></div>;
};

export default RegisterForm;
